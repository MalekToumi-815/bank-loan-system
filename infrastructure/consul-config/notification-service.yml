spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: auto
        concurrency: 1
        max-concurrency: 3

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 4f7baa9c480282
    password: 95ede2c76c50d1
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# This allows Consul to hit the health check URL successfully
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
    
# Frontend URL (from .env) - used by services that need the Angular app URL
angular_app_url: ${angular_app_url:http://localhost:4200}